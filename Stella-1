
fill(160);
rectMode(CENTER);
rect(160, 160, 30, 30);
var z = 20;
colorMode(HSB);

while(z < height) {
  {
  strokeWeight(20);
  stroke(random(560),random(460),random(786));
  noFill();
  rect (170, 170, z, z);
  }
  z = z + 25;
}
